import React from "react";

const Header = () => {
  return (
    <div
      style={{
        minHeight: "3rem",
        backgroundColor: "skyblue",
        padding: "1rem",
        marginBottom: "1rem",
      }}
    >
      Header
    </div>
  );
};

export default Header;
